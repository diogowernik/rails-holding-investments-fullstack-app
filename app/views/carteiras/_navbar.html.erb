<nav class="navbar navbar-expand-lg topnavbar mb-4">
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
      <a href="#" class="navbar-brand dropdown dropdown-list" data-toggle="dropdown"><%= @carteira.nome %></a>
      <div class="dropdown-menu ">
            <div class="dropdown-item">
               <div class="list-group">
                   <% @carteiras.each do |carteira| %>
                        <% if carteira.nome != @carteira.nome %>  
                          <div class="list-group-item list-group-item-action">
                             <%= link_to carteira_path(carteira.id) do %>
                                 <% carteira.nome %>
                            <% end %>
                          </div>
                        <% end %>
                    <% end %>
    
               </div>
            </div>
         </div>
  <div class="collapse navbar-collapse" id="navbarNavDropdown">
    <ul class="nav navbar-nav mr-auto flex-column flex-lg-row"></ul>
    <ul class="navbar-nav flex-row">
      <li class="nav-item active">
        <%= link_to carteira_path(@carteira), class: 'nav-link' do %>
             Mov. de Derivativos
        <% end %>
      </li>
      <li class="nav-item">
        <%= link_to radar_puts_path(@carteira), class: 'nav-link' do %>
             Radar Puts
        <% end %>
      </li>
      <li class="nav-item">
            <%= link_to radar_calls_path(@carteira), class: 'nav-link' do %>
                 Radar Call
            <% end %>
      </li>
    </ul>
  </div>
</nav>