<%= simple_form_for([:admin, ano]) do |form| %>
  <% if ano.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ano.errors.count, "error") %> prohibited this ano from being saved:</h2>

      <ul>
      <% ano.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<div class="row">
  <div class="col-md-6">
    <%= form.label :ano %>
    <%= form.number_field :ano %>
  </div>

</div>
<div class="row">
    <div class="col-md-3 mt-4">
		<div class="form-group">
                <%= form.submit 'Enviar', class: 'btn btn-block btn-primary' %>
         </div>
     </div>
</div>
<% end %>
