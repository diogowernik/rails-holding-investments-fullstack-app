<%= form_with(model: investimento, local: true) do |form| %>
  <% if investimento.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(investimento.errors.count, "error") %> prohibited this investimento from being saved:</h2>

      <ul>
      <% investimento.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<div class="row">
  <div class="col-md-4 mt-3">
    <label>Ativo</label>
    <%#= form.text_field :ativo_id, class: 'form-control' %>
    <%= form.select :ativo_id, Ativo.all.collect {|d| [d.ticker, d.id]}, {}, {class: 'form-control'} %>
  </div>
  <div class="col-md-4 mt-3">
    <%= form.label :carteira_id %>
    <%= form.select :carteira_id, Carteira.all.collect {|d| [d.nome, d.id]}, {}, {class: 'form-control'} %>
  </div>
  
  <div class="col-md-4 mt-3">
    <%= form.label :tipo_id %>
    <%= form.select :tipo_id, Tipo.all.collect {|d| [d.nome, d.id]}, {}, {class: 'form-control'} %>
  </div>
  
  <div class="col-md-4 mt-3">
    <%= form.label :corretora_id %>
    <%= form.select :corretora_id, Corretora.all.collect {|d| [d.nome, d.id]}, {}, {class: 'form-control'} %>
  </div>

  <div class="col-md-4 mt-3">
    <%= form.label :quantidade %>
    <%= form.number_field :quantidade, class: 'form-control' %>
  </div>

  <div class="col-md-4 mt-3">
    <%= form.label :valor_medio %>
    <%= form.number_field :valor_medio, class: 'form-control' %>
  </div>

</div>
<div class="row">
	<div class="col-md-3 mt-4">
		<div class="form-group">
			<%= form.submit 'Enviar', class: 'btn btn-block btn-primary' %>
		</div>
	</div>
</div>
<% end %>
