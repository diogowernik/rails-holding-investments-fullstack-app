<%= form_with(model: carteira, local: true) do |form| %>
  <% if carteira.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(carteira.errors.count, "error") %> prohibited this carteira from being saved:</h2>

      <ul>
      <% carteira.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="row">
    <div class="col-md-6">
      <label>Nome da Carteira</label>
      <%= form.text_field :nome, class: 'form-control' %>
    </div>

    <div class="col-md-6">
      <label>Gestor</label>
      <%= form.select :user_id, User.all.collect {|d| [d.email, d.id]}, {}, {class: 'form-control'} %>
    </div>
</div>
<div class="row">
    <div class="col-md-3 mt-4">
		<div class="form-group">
                <%= form.submit 'Enviar', class: 'btn btn-block btn-primary' %>
         </div>
     </div>
</div>
<% end %>
