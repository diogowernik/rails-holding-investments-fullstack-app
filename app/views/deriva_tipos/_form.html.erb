<%= form_with(model: deriva_tipo, local: true) do |form| %>
  <% if deriva_tipo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(deriva_tipo.errors.count, "error") %> prohibited this deriva_tipo from being saved:</h2>

      <ul>
      <% deriva_tipo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :tipo %>
    <%= form.text_field :tipo %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
